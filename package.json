{
  "name": "gypsum",
  "scripts": {
    "setup-playwright": "sudo rm -f /etc/apt/sources.list.d/yarn.list && npm install && npx playwright install --with-deps chromium",
    "test": "CODESPACE_NAME= PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers npx playwright test tests/1-data",
    "test:behaviour": "CODESPACE_NAME= PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers npx playwright test tests/1-data tests/2-behaviour",
    "test:all": "CODESPACE_NAME= PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.56.1"
  }
}
